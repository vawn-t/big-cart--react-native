import { StyleSheet } from 'react-native';

// Themes
import { variantStyle, fontWeightStyle } from '@themes/index';

const styles = StyleSheet.create({ ...variantStyle, ...fontWeightStyle });

export default styles;
